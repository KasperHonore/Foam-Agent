# Code owners are auto-requested for review on any PR that touches matching paths.
# With branch protection requiring PRs into `main`, this makes sure the
# maintainer is always pinged on incoming changes.

*       @KasperHonore
